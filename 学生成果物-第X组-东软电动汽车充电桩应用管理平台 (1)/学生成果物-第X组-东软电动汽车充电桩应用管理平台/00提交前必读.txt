东软电动汽车充电桩应用管理平台 V1.2——提交前必读

本成果包包含：第一阶段 Qt 用户端/管理端/SQLite/TCP 源码，V1.2 会话安全、整数金额、峰谷计费、月卡、头像、客服反馈、持久审计日志与并发防护，第二阶段数据聚合、Ridge 负荷预测与 ECharts 大屏，以及需求矩阵、概要设计、60 条测试用例、团队贡献度表、路演 PPT、环境影响报告和项目总结报告。

下列信息只有项目成员本人能够真实提供，已保留为待填写项，请勿带占位内容直接提交：
1. 把“第X组、组长姓名、待填写姓名、待填写学号、成员A～E”替换为真实信息。
2. 在团队贡献度表中按真实分工调整比例，合计必须为 100%，且不能平均分配。
3. 在课程 Ubuntu/Qt 环境实际编译并执行测试，把“待脚本执行、待人工执行、待回归”改为真实结果。
4. 将项目总结报告和路演 PPT 的截图占位框替换为真实运行截图。
5. 录制个人 MP4 和小组 MP4；录屏必须显示真实程序操作，不得使用设计稿代替运行结果。
6. 将源码 `.github/CODEOWNERS` 中的示例账号替换为组员真实 GitHub 用户名。

升级与回归顺序：
1. 停止旧服务端，备份现有 `charging_platform.db` 及同级 `avatars/` 目录。
2. 用新版完整替换 `~/ChargingPlatform` 源码；客户端与服务端必须同时更新。
3. 执行 `./scripts/build.sh`，然后先运行 `./build/bin/charging_server`，再运行一个或多个 `./build/bin/charging_client`。
4. 管理员账号仍为 `admin / 123456`；旧库首次启动会自动把管理员明文密码迁移为 PBKDF2 哈希。
5. 先用独立数据库运行：`python3 scripts/static_database_test.py`，再运行 `python3 scripts/acceptance_test.py --db data/v12-acceptance.db`。
6. 人工复核头像上传、月卡/客服页、管理端审计页、跨时段与跨午夜计费。
7. 运行 `analytics/generate_dashboard.py`，并用本地 HTTP 服务打开 `analytics/dashboard`。
8. 按 `03测试用例-第X组.xlsx` 逐条记录实际结果，完成身份、截图和录屏后再压缩提交。

重要说明：
- 运行数据库、头像、构建目录、`.env`、证书和个人 Qt Creator 配置不得提交到 Git。
- 手机号免验证码登录仍是课程演示机制；生产部署必须接入短信验证码或其他强认证。
- 当前执行环境没有 Qt 开发套件，因此已完成源码、SQLite 结构、Python、JSON、文档和工作簿静态检查，但最终 qmake/make、界面操作和真人录屏必须在课程 Ubuntu 虚拟机完成，不能把“待执行”伪报为通过。
